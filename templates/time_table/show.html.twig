{% extends 'base.html.twig' %} {% block title %}TimeTable{% endblock %} {% block
body %}
<div class="container">

  <h1 class="worspace">Voir l'horaire</h1>

  <table class="table">
    <tbody>
      <tr>
        <th>Id</th>
        <td>{{ time_table.id }}</td>
      </tr>
      <tr>
        <th>Name</th>
        <td>{{ time_table.name }}</td>
      </tr>
    </tbody>
  </table>

  <a class="btn btn-success mb-3 me-3" href="{{ path('app_time_table_index') }}">back to list</a>

  <a class="btn btn-primary mb-3" href="{{ path('app_time_table_edit', { id: time_table.id }) }}">edit</a>

  {{ include("time_table/_delete_form.html.twig") }}
</div>

{% endblock %}