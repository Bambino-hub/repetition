{% extends 'base.html.twig' %} {% block title %}Edit TimeTable{% endblock %} {%
block body %}
<div class="container">
  <h1 class="worspace workspace1">Editer les horaires de travailles</h1>
  <div class="mb-4">
    {{ include("time_table/_form.html.twig", { button_label: "Update" }) }}
  </div>
  <div class="d-flex">
    <a
      class="btn btn-success me-5 workspace1"
      href="{{ path('app_time_table_index') }}"
      >back to list</a
    >

    {{ include("time_table/_delete_form.html.twig") }}
  </div>
</div>
{% endblock %}
