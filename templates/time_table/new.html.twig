{% extends 'base.html.twig' %} {% block title %}New TimeTable{% endblock %} {%
block body %}
<div class="container">
  <di class="form">
    <h1 class="worspace">Créer une horaire</h1>

    <div class="mb-4">
      {{ include("time_table/_form.html.twig") }}
    </div>

    <a class="btn btn-success" href="{{ path('app_time_table_index') }}"
      >back to list</a
    >
  </di>
</div>

{% endblock %}
