{% extends "base.html.twig" %} {% block title %}liste des inscrits{% endblock %}
{% block body %}

<div class="container">

    <h1 class="worspace">Liste des inscrits</h1>
    {% for user in users %}
    <h2>Numero: {{ user.id }}</h2>
    <table class="table table-success table-striped">
        <thead>
            <th>Nom</th>
            <th>Prénom</th>
            <th>Contact</th>
            <th>Action</th>
        </thead>
        <tbody>
            <tr>
                <td>{{ user.lastname }}</td>
                <td>{{ user.firstname }}</td>
                <td>{{ user.telephone }}</td>
                <td>{% include "home/_delete_form.html.twig" %}</td>
            </tr>
        </tbody>
        <table>
        </table>
        {% endfor %}
</div>

{% endblock %}