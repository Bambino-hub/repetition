{% extends 'base.html.twig' %} {% block title %}Edit Matter{% endblock %} {%
block body %}
<div class="container">
  <h1 class="worspace workspace1">Editer la Matière</h1>

  <div class="mb-4">
    {{ include("matter/_form.html.twig", { button_label: "Update" }) }}
  </div>
  <div class="d-flex">
    <a
      class="btn btn-success me-5 workspace1"
      href="{{ path('app_matter_index') }}"
      >back to list</a
    >

    {{ include("matter/_delete_form.html.twig") }}
  </div>
</div>

{% endblock %}
