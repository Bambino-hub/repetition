{% extends 'base.html.twig' %} {% block title %}Matter{% endblock %} {% block
body %}
<div class="container">
  <h1 class="worspace">Matter</h1>

  <table class="table">
    <tbody>
      <tr>
        <th>Id</th>
        <td>{{ matter.id }}</td>
      </tr>
      <tr>
        <th>Name</th>
        <td>{{ matter.name }}</td>
      </tr>
    </tbody>
  </table>

  <a class="btn btn-success mb-4 me-5" href="{{ path('app_matter_index') }}"
    >back to list</a
  >

  <a
    class="btn btn-primary mb-4"
    href="{{ path('app_matter_edit', { id: matter.id }) }}"
    >edit</a
  >

  {{ include("matter/_delete_form.html.twig") }}
</div>

{% endblock %}
