{% extends 'base.html.twig' %} {% block title %}Matter index{% endblock %} {%
block body %}
<div class="container">
  <h1 class="worspace">Les matières</h1>

  <table class="table">
    <thead>
      <tr>
        <th>Id</th>
        <th>Name</th>
        <th>actions</th>
      </tr>
    </thead>
    <tbody>
      {% for matter in matters %}
      <tr>
        <td>{{ matter.id }}</td>
        <td>{{ matter.name }}</td>
        <td>
          <a
            class="btn btn-secondary"
            href="{{ path('app_matter_show', { id: matter.id }) }}"
            >show</a
          >
          <a
            class="btn btn-success"
            href="{{ path('app_matter_edit', { id: matter.id }) }}"
            >edit</a
          >
        </td>
      </tr>
      {% else %}
      <tr>
        <td colspan="3">no records found</td>
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <a class="btn btn-success" href="{{ path('app_matter_new') }}">Create new</a>
</div>

{% endblock %}
