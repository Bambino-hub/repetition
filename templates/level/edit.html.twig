{% extends 'base.html.twig' %} {% block title %}Edit Level{% endblock %} {%
block body %}
<div class="container">
  <h1 class="worspace workspace1">Editer une classe</h1>
  <div class="mb-4">
    {{ include("level/_form.html.twig", { button_label: "Update" }) }}
  </div>
  <div class="d-flex">
    <a
      class="btn btn-success me-5 workspace1"
      href="{{ path('app_level_index') }}"
      >back to list</a
    >

    {{ include("level/_delete_form.html.twig") }}
  </div>
</div>

{% endblock %}
