{% extends 'base.html.twig' %} {% block title %}Level{% endblock %} {% block
body %}
<div class="container">
  <h1 class="worspace">La classe</h1>

  <table class="table">
    <tbody>
      <tr>
        <th>Id</th>
        <td>{{ level.id }}</td>
      </tr>
      <tr>
        <th>Name</th>
        <td>{{ level.name }}</td>
      </tr>
    </tbody>
  </table>

  <a class="btn btn-success me-5 mb-4" href="{{ path('app_level_index') }}"
    >back to list</a
  >

  <a
    class="btn btn-primary mb-4"
    href="{{ path('app_level_edit', { id: level.id }) }}"
    >edit</a
  >

  {{ include("level/_delete_form.html.twig") }}
</div>

{% endblock %}
