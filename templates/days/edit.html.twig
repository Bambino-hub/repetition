{% extends 'base.html.twig' %} {% block title %}Edit Days{% endblock %} {% block
body %}
<div class="container">
  <h1 class="worspace workspace1">Editer un jour de travaille</h1>

  {{ include("days/_form.html.twig", { button_label: "Update" }) }}

  <div class="d-flex">
    <a
      class="btn btn-success me-5 workspace1"
      href="{{ path('app_days_index') }}"
      >back to list</a
    >
    {{ include("days/_delete_form.html.twig") }}
  </div>
</div>

{% endblock %}
