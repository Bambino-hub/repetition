{% extends 'base.html.twig' %} {% block title %}Days{% endblock %} {% block body
%}
<div class="container">
  <h1 class="worspace">jour</h1>

  <table class="table">
    <tbody>
      <tr>
        <th>Id</th>
        <td>{{ day.id }}</td>
      </tr>
      <tr>
        <th>Name</th>
        <td>{{ day.name }}</td>
      </tr>
    </tbody>
  </table>

  <a class="btn btn-success mb-4 me-5" href="{{ path('app_days_index') }}"
    >back to list</a
  >

  <a
    class="btn btn-primary mb-4"
    href="{{ path('app_days_edit', { id: day.id }) }}"
    >edit</a
  >

  {{ include("days/_delete_form.html.twig") }}
</div>

{% endblock %}
